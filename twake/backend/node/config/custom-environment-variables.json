{
  "websocket": {
    "auth": {
      "jwt": {
        "secret": "AUTH_JWT_SECRET"
      }
    },
    "php_pusher_secret": "WS_PHP_PUSHER_SECRET"
  },
  "auth": {
    "jwt": {
      "secret": "AUTH_JWT_SECRET"
    }
  },
  "database": {
    "type": "DB_DRIVER",
    "mongodb": {
      "uri": "DB_MONGO_URI",
      "database": "DB_MONGO_DATABASE"
    },
    "cassandra": {
      "contactPoints": "DB_CASSANDRA_URI",
      "localDataCenter": "DB_CASSANDRA_LOCALDATACENTER",
      "database": "DB_CASSANDRA_KEYSPACE"
    }
  }
}
