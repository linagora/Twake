{
  "services": ["auth", "user", "channels", "webserver", "websocket", "database", "realtime"],
  "webserver": {
    "port": 3000,
    "logger": {
      "level": "info"
    },
    "cors": {
      "origin": "*"
    }
  },
  "websocket": {
    "path": "/socket/",
    "adapters": {
      "types": [],
      "redis": {
        "host": "redis",
        "port": 6379
      }
    },
    "auth": {
      "jwt": {
        "secret": "supersecret"
      }
    },
    "php_pusher_secret": "pusher_secret"
  },
  "auth": {
    "jwt": {
      "secret": "supersecret"
    }
  },
  "database": {
    "type": "mongodb",
    "mongodb": {
      "uri": "mongodb://localhost:27017",
      "database": "twake"
    },
    "cassandra": {
      "contactPoints": ["scylladb:9042"],
      "localDataCenter": "datacenter1",
      "keyspace": "twake",
      "wait": false,
      "retries": 10,
      "delay": 200
    }
  }
}
