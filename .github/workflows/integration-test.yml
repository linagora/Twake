name: integration-test

on:
  pull_request:
    branches: [main, develop, qa, canary]
    paths:
      - "twake/**"

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: cd twake && docker build -t integration-test -f docker/integration-test/Dockerfile .
      - name: run-twake-instance
        run: cd twake && docker-compose -f docker-compose.dev.mongo.yml run node npm run install && docker-compose -f docker-compose.dev.mongo.yml up -d && sleep 60
      - name: get-logs
        run: cd twake && docker-compose -f docker-compose.dev.mongo.yml logs && curl https://localhost:3000
      - name: run
        run: cd twake && docker run --network host -e SERVER=http://localhost:3000 integration-test
